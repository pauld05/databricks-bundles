resources:
  schemas:
    bronze:
      name: "bundle_bronze"
      catalog_name: "${bundle.target}_playground"
      comment: Raw data (bronze)

    silver:
      name: "bundle_silver"
      catalog_name: "${bundle.target}_playground"
      comment: Cleaned and enriched data (silver)

    gold:
      name: "bundle_gold"
      catalog_name: "${bundle.target}_playground"
      comment: Data ready for analytics consumption (gold)