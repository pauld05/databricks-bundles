resources:
  jobs:
    job_example:
      name: job-example-bundle
      tasks:
        - task_key: task_bundle-cumbap
          notebook_task:
            notebook_path: ../../notebooks/transformation_notebook.ipynb
            base_parameters:
              env: "${bundle.target}"